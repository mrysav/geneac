{
  "name": "Geneac",
  "description": "Genealogy for Brainiacs!",
  "keywords": [
    "genealogy", "rails", "facebook", "family", "history"
  ],
  "repository": "https://github.com/mrysav/geneac",
  "success_url": "/",
  "scripts": {
    "postdeploy": "bundle exec rake db:migrate"
  },
  "env": {
    "SECRET_KEY_BASE": {
      "description": "A secret key for verifying the integrity of signed cookies.",
      "generator": "secret"
    },
    "HOSTNAME": {
      "description": "Host to link to in password reset or confirmation emails."
    },
    "AWS_ACCESS_KEY_ID": {
      "description": "Access key ID for AWS Account for S3 file storage."
    },
    "AWS_SECRET_ACCESS_KEY": {
      "description": "Secret access key for AWS Account for S3 file storage."
    },
    "AWS_BUCKET_NAME": {
      "description": "S3 bucket name for file storage."
    },
    "AWS_REGION": {
      "description": "Region that S3 bucket is located in."
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "free"
    }
  },
  "image": "heroku/ruby",
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/ruby"
    },
    {
      "url": "https://github.com/heroku/heroku-buildpack-activestorage-preview"
    }
  ],
  "environments": {
    "test": {
      "scripts": {
        "test": "bundle exec rake test"
      }
    }
  }
}