<%
# Tag list partial
# For use with acts_as_taggable
%>

<% if field.data.present? %>
<div class="taglist">
  <% field.data.each do |tag| %>
    <span class="tag"><%= tag %></span>
  <% end %>
</div>
<% end %>
